import Layout from '@/components/Layout.tsx';
import Menu from '@/components/Menu.tsx';

const HomePage = () => {
  return (
    <Layout>
      <Menu />
    </Layout>
  );
};

export default HomePage;
